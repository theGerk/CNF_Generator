Will generate just random characters to act as variables, space is used to represent a break between clauses and new line is used to represent break between staements.

It will only uses characters between 33 and 126 as well as possibly using 128 through 254 as variables.

